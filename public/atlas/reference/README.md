# Authentic 2D reference plates

These locally bundled source previews are shown by HumanBody’s **Real reference**
2D mode. Attribution, source URLs, licences, and the resize/change notice are
listed in [`../../ATTRIBUTION.md`](../../ATTRIBUTION.md).

Do not replace these images with uncredited search results. The interactive SVG
plate is generated separately from the app’s teaching fallback geometry.
